# terraform-ansible-project
To make the magic go brrrr, one needs to create and download a gcp security key, and to create a new ssh key with ssh-keygen tool and put all 3 of them into ./key/ directory.
